# 수치와 증거 중심 문서 규칙

## 목적

이 프로젝트의 설명은 “빠르다”, “좋다”, “많다”, “거의 같다” 같은 표현만으로 결론을 내리지 않습니다. 측정 가능한 내용은 값과 단위를 기록하고, 결론을 확인할 수 있는 로그·명령·파일·공식 문서를 함께 남깁니다.

## 적용 범위

이 규칙은 다음 기록에 적용합니다.

- 메인 `README.md`
- `docs/` 아래 설계·결정·문제 해결 문서
- 테스트와 성능 확인 결과
- 모델·라이브러리 선택 근거
- 사용자에게 전달하는 작업 완료 설명

## 필수 규칙

1. 측정 가능한 값에는 숫자와 단위를 함께 씁니다.
   - 시간: `159.6초`
   - 이미지 크기: `768×1344픽셀`
   - GPU 메모리: `8.0GB`
   - 파일 수: `24개`
   - 테스트 결과: `11개 통과, 0개 실패`

2. 숫자에는 측정 조건을 함께 씁니다.
   - GPU 이름
   - 모델과 버전
   - 입력 이미지 크기
   - 반복 횟수
   - 실행 날짜
   - 사용한 설정 파일

3. 결론에는 확인 근거를 연결합니다.
   - 로그 파일 경로와 실행 ID
   - 재현 명령
   - 테스트 파일과 테스트 이름
   - 설정 파일과 설정값
   - 공식 문서 링크
   - 사용자 수동 평가 결과

4. 전후 비교에는 두 값을 모두 씁니다.
   - 잘못된 예: “실행 속도가 빨라졌습니다.”
   - 올바른 형식: “동일 입력에서 실행 시간이 변경 전 160초, 변경 후 95초로 65초 감소했습니다.”

5. 측정하지 않은 값은 추측하지 않습니다.
   - `미측정`
   - `확인 불가`
   - `표본 부족`
   - `사용자 평가 전`

6. 주관적인 품질은 평가 기준과 표본 수를 함께 씁니다.
   - 평가 항목: 얼굴 유지, 의상 유지, 손 모양, 신체 비율
   - 평가 척도: 항목별 1~5점
   - 표본 수: 생성 이미지 3장
   - 통과 기준: 3장 중 2장 이상이 항목별 4점 이상

7. 한 번의 성공을 전체 성공으로 확대하지 않습니다.
   - 공식 예제 1장 성공
   - 실제 사용자 입력 1장 실패
   - 대표 입력 검증 상태: `표본 부족`

8. 모델과 라이브러리는 정확한 식별자를 씁니다.
   - 모델 저장소 ID
   - 파일명
   - 버전
   - 계산 형식
   - 라이선스
   - 설치 위치

## 기록 형식

```text
측정 대상:
측정값:
단위:
입력 조건:
실행 환경:
실행 날짜:
확인 방법:
근거 위치:
판정:
남은 미측정 항목:
```

## 오류 기록 형식

```text
오류 발생 횟수:
성공 횟수:
실패 단계:
입력 파일:
선택 설정:
오류 메시지:
실행 ID:
로그 위치:
재현 여부:
수정 후 결과:
```

## 수치로 표현할 수 없는 내용

구조의 목적이나 사용자 결정처럼 숫자로 직접 표현할 수 없는 내용은 숫자를 만들지 않습니다. 대신 결정 날짜, 관련 파일, 선택한 방법, 제외한 방법과 확인 근거를 기록합니다.

예:

```text
결정: 자세 제어 기능(ControlNet) 보류
결정 날짜: 2026-08-25
선택 근거: 다른 참조 이미지 시험에서 캐릭터 유지 결과가 개선됨
시험 이미지 수: 2장
정량 평가: 미측정
다음 확인 조건: 자세 참조 단계 시작 전 비교 시험
```

## 완료 보고 기준

작업 완료 설명에는 최소한 다음 값을 포함합니다.

- 변경 파일 수
- 추가·삭제된 코드 줄 수 또는 변경 범위
- 실행한 테스트 수와 성공·실패 수
- 실행하지 않은 테스트
- 실제 GPU 생성 실행 여부
- 커밋 ID와 원격 브랜치
- 아직 측정하지 않은 품질 항목
